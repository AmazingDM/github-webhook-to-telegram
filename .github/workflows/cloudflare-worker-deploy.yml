name: Cloudflare Worker Deploy

on:
  push:
    tags:
      - "*"
  workflow_dispatch:

concurrency:
  group: cloudflare-worker-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read

    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
      HOOK_CONFIG_JSON: ${{ secrets.HOOK_CONFIG_JSON }}

    steps:
      - uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate deployment secrets
        shell: bash
        run: |
          for name in CLOUDFLARE_API_TOKEN CLOUDFLARE_ACCOUNT_ID BOT_TOKEN HOOK_CONFIG_JSON; do
            if [ -z "${!name}" ]; then
              echo "Missing required secret: $name" >&2
              exit 1
            fi
          done

      - name: Type check
        run: pnpm typecheck

      - name: Build
        run: pnpm build

      - name: Test
        run: pnpm test

      - name: Deploy Worker
        run: pnpm run deploy

      - name: Sync Worker secrets
        shell: bash
        run: |
          printf '%s' "$BOT_TOKEN" | pnpm exec wrangler secret put BOT_TOKEN
          printf '%s' "$HOOK_CONFIG_JSON" | pnpm exec wrangler secret put HOOK_CONFIG_JSON
